pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Pulls the code from my repo
                git url: 'https://github.com/Pavithrap7/to_do_app.git', 
                    branch: 'master'
            }
        }
}
}
